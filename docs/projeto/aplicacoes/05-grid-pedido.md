# 05 — Pedidos (grid + form mestre-detalhe)

| Campo | Valor |
|-------|-------|
| Rota | `/pedidos` |
| API | `/api/pedidos` |
| Status | OK |

CRUD de `pedido` com itens (`pedido_item`): cliente, status (`rascunho`/`confirmado`/`cancelado`), observação, linhas produto/qtd/preço, total recalculado.
